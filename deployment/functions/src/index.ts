import { initializeApp } from "firebase-admin/app";

initializeApp();

export { deviceCode } from "./deviceCode";
export { deviceToken } from "./deviceToken";
export { activateDevice } from "./activateDevice";
export { transcribe } from "./transcribe";
